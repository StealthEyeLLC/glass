# Implementation status

**Spec:** `GLASS_FULL_ENGINEERING_SPEC_v10.md` (locked)  
**Plan:** `GLASS_V0_BUILD_PLAN.md`

## Vertical Slice v0 / v1 / v2 (milestone)

**Vertical Slice v0** is the first end-to-end **bounded** demo path: **Tier B static replay** (default) and **`?live=1`** present the **same** Glass Scene System v0 + Drawable Primitives story (replace / append / resync emphasis, bounded tail density, HTTP reconcile chip — **no** invented graph). **Vertical Slice v1** deepens the **same** path: richer **zones** and **fact cards**, a **bounded state rail** under the primary band (`state_rail_*` / `replay_*` drawable tags), **`snapshotOriginLabel`** + optional HTTP body origin wiring in **`liveSessionShell`**, **`replayPrefixFraction`** for replay, and **`stripSource`** on **`LiveVisualSpec`**. **Vertical Slice v2** adds **`clusters`** on **`GlassSceneV0`** (bounded **actor/sample** groupings from **`kind`** counts + honest system/snapshot/replay facts — see **`boundedActorClusters.ts`**), **`actor_cluster_*`** / **`actor_cluster_emphasis_bar`** drawable tags, and **`actorClusterSummaryLine`** on **`LiveVisualSpec`** for overlay text — still **not** topology. **Vertical Slice v3** adds **`regions`** (**`SceneBoundedRegion`** + **`boundedSceneRegions.ts`**) and **`applyBoundedSceneComposition`** (`composition_*` drawable tags, outer frame stroke, **`boundedCompositionCaption`** on **`LiveVisualSpec`**) so the strip reads as one **bounded execution** composition (panels / accents / separator — **not** graph edges). **Vertical Slice v4** adds **`emphasis`** (**`boundedSceneEmphasis.ts`**, **`computeBoundedSceneEmphasis`**, replay presentation fields, **`applyBoundedEmphasisOverlays`**, **`emphasis_*`** drawable tags, **`boundedEmphasisSummaryLine`** on **`LiveVisualSpec`**) — bounded pulse/flash when basis changes between compiles (**`previousEmphasis`** from replay/live shells), **not** decorative idle motion. **Vertical Slice v5** adds **`boundedSceneSelection.ts`** — deterministic **`glass.sel.v0:*`** ids, primitive + overlay **hit targets**, **`buildBoundedInspectorLines`**, Canvas **selection outline**, and replay/live **pointer** interaction (**no** GPU picking; shared layout map). **Vertical Slice v6** adds **`boundedSceneFocus.ts`** — **`computeBoundedSceneFocus`** (pure **`BoundedSceneFocusV0`**), **`applyBoundedSceneFocusToPrimitives`** for band/cluster dimming + honest emphasis frames, **`LiveVisualSpec.boundedFocusCaptionLine`**, and provenance **`boundedFocusSummary`** — selection visibly reshapes the strip **without** inferred topology (**not** graph drill-down). **Vertical Slice v7** adds **`boundedSceneFocusReflow.ts`** — **`computeBoundedStripLayoutFromFocus`** + **`BoundedStripLayoutV0`** threaded through Drawable Primitives (**primary / rail / cluster** heights and optional lane width fractions); **`LiveVisualSpec.boundedStripReflowLine`**, **`stripPrimaryY`**, **`stripContentBottomY`** — spatial reflow, not a second scene. **Vertical Slice v8** adds **`boundedSceneCompare.ts`** — **`computeBoundedSceneCompare`** + **`compare_overlay_*`** primitives + **`LiveVisualSpec`** compare fields; replay/live pass **`previousScene`** (one honest step/paint back) — **not** multi-frame history, **not** topology diff. **Vertical Slice v9** adds **`boundedEvidenceDrilldown.ts`** — **`computeBoundedEvidenceDrilldown`** + **`renderBoundedEvidenceInto`**; surfaces a **small bounded list** of tail/prefix **rows** + **facts** tied to selection/compare — **not** a full trace, **not** causality. **Vertical Slice v10** adds **`boundedSceneCrosslink.ts`** — evidence **row keys** + **compare** overlay targets map to the **same** selection ids as the scene; shells wire **bidirectional** activation (replace/toggle) with honest unmappable notes — **not** graph navigation, **not** fake causality. **Vertical Slice v11** adds **`boundedTemporalLens.ts`** — a capped **viewer-held** ring of recent bounded **paints** + replay **step neighborhood** chips; **compare baseline** can be **immediate prior** or another ring entry; **not** server history, **not** full timeline. **Vertical Slice v12** adds **`boundedEpisodes.ts`** + **`boundedEpisodesPanel.ts`** — rule-based **episode cards** (≤4) from **`BoundedSceneCompareV0`** + scene facts + immediate prior paint (replay cursor) + **WS tail mutation** (live) — **not** AI narrative, **not** causal topology. **Vertical Slice v13** adds **`boundedClaims.ts`** + **`boundedClaimsPanel.ts`** — **bounded claim chips** + **receipt** from the same compare + evidence + episode stack (**`BoundedClaimStatusV0`**, per-kind **does-not-imply** lines) — **not** generic reports, **not** legal provenance. **Vertical Slice v14** tightens **`glass.receipt.v0`** — deterministic receipt ids, **fact/row ref keys**, ordered **support bullets**, **focus** line (selection + episode), **primary claim** bias for cluster selection when honest, evidence-row **highlight** for mechanical claim support — **not** AI, **not** causal theater. **Vertical Slice v15** adds **`tests/fixtures/canonical_scenarios_v15/`** — four small **synthetic** Tier B packs (replace/append/calm/file) + **live** resync/warning tests in **`canonicalScenariosV15.integration.test.ts`**, **`npm run fixture:canonical-scenarios-v15`** / **`verify:canonical-scenarios-v15`**, **`integration_tests`** presence check — **not** a scenario theater, **not** extra product surfaces. **Vertical Slice v16** is **presentation-only** for the same bounded trust surfaces: **`boundedTrustSurfaces.css`**, structured receipt DOM (**`data-trust-tier`**, section markers), evidence trust wrapper + row-label captions — **not** new claim/receipt semantics, **not** wire changes. **Vertical Slice v17** tightens **live** hybrid **WebGPU + Canvas overlay** parity: selection highlight on the overlay canvas receives the same **`previousScene`** compare baseline as geometry and hit-testing; **`paintLiveVisualSurface`** and **`buildBoundedSelectionHitTargetsForScene`** thread explicit **`computeBoundedSceneCompare`** into **`liveVisualSpecFromScene`**; live provenance **`buildCurrentLiveVisualSpec`** matches — **not** new drawable tags, **not** bridge changes. **Vertical Slice v18** defines the **flagship bounded demo path**: **`canonical_v15_append_heavy.glass_pack`** (see **`docs/VERTICAL_SLICE_V0.md`**); replay/live copy and a **flagship callout** prioritize that path; dev **`?fixture=flagship`** loads it; **`vertical_slice_v0`** remains minimal CI — **not** a second compiler, **not** wire changes. **Vertical Slice v19** hardens **audit readiness** on that path: **temporal compare-baseline** changes reset bounded **episode** + **claim** UI state and evidence **crosslink** notes, and suppress **primary-claim** chip auto-highlight until the next **cursor/tail-driven** paint; **claim chip** status uses **`formatBoundedClaimChipStatusShort`**; live **visual intro / fallback** copy aligns with replay — **not** wire changes. **Vertical Slice v20** closes **external-style audit** gaps: explicit **reading-order** copy (replay **`replay-reading-order`** + live **`live-reading-order`**), clearer **empty receipt** default + **`emptySupplementLine`** after temporal baseline handoff, and documented alignment between flagship and **canonical scenario** breadth — **not** launch claims, **not** wire changes. **Vertical Slice v21** pushes **freeze-candidate** flagship coherence: **`GLASS_FLAGSHIP_CHAIN_ONE_LINER`** / **`GLASS_FLAGSHIP_CHAIN_DOC`** woven into reading-order strings and a **flagship callout** framing paragraph (**`replay-flagship-framing`**); **integration** coverage ensures the temporal-baseline **empty-receipt supplement** stays aligned with **`RECEIPT_EMPTY_SUPPLEMENT_AFTER_TEMPORAL_BASELINE`** — **not** wire changes, **not** new claim semantics. **Vertical Slice v22** closes a **replay vs live** flagship gap: **`live-flagship-framing`** carries the same **`GLASS_FLAGSHIP_CHAIN_DOC`** as replay; docs record an explicit **freeze-candidate verdict** (**showcase path only** — not production ingest / **F-IPC** / Phase-6). **`liveSessionShell.test.ts`** asserts live framing text matches the constant. **Vertical Slice v23** prepares **public open-source** reading order: root **`VISION.md`**, README **doc order** + public-first copy, **`docs/media/`** placeholders, **`.github/ISSUE_TEMPLATE/bounded_showcase.md`** — **not** wire changes, **not** new subsystems. **Vertical Slice v24** tightens the **GitHub landing** story: README opens with **hook + standout chain**, flagship/breadth separation, **`docs/media`** ordered asset spec + checklists — **not** wire changes. **Vertical Slice v25** adds **committed** **`docs/media/`** PNGs (**`01`–`04`**) and a root README **Screenshots** table — **not** wire changes, **not** new product features. Viewer copy lives in `viewer/src/app/verticalSliceV0.ts`; replay and live shells share a **hero** layout so the **scene canvas** reads as the primary payoff. **Known-good fixture:** `tests/fixtures/vertical_slice_v0/glass_vertical_slice_v0_tier_b.glass_pack` (synthetic Tier B pack; `npm run fixture:vertical-slice` / `npm run verify:vertical-slice-fixture` from `viewer/`). **Dev-only:** `?fixture=vertical_slice_v0` with `npm run dev` auto-loads that pack via `viewer/vite.config.ts` middleware — **not** in static/prod output. Details: **`docs/VERTICAL_SLICE_V0.md`**, **`tests/fixtures/vertical_slice_v0/README.md`**. **Next major step:** durable push ingest / additive live-era HTTP+WS — see “Next engineering steps” below; **F-IPC transport** remains provisional.

## Present in repo (bootstrap complete)

| Area | State |
|------|--------|
| Workspace | Root `Cargo.toml` workspace + `viewer/` npm package |
| Phase 1 spine | `session_engine`: events, manifest, ZIP **`glass.pack.v0.scaffold`** (JSONL, Tier B viewer) + **`glass.pack.v0.scaffold_seg`** (`events.seg` binary v1: magic `GLSSG001` + length-prefixed JSON), standalone `.seg` file I/O (`SessionLog::from_seg_path` / `write_seg_path`), append session log, **pure** `sanitization` (export path only) |
| Schema | `schema/glass_event_schema.json`, `schema/examples/minimal_event.json`; TS codegen not wired |
| Viewer Scene System v0 | `viewer/src/scene/`: **`glass.scene.v0`** — pure bounded `GlassSceneV0` (zones, nodes, optional honest edges, sample scope, **`regions`**, **`emphasis`** / replay presentation fields for Vertical Slice v0–v4); **`compileLiveToGlassSceneV0`** (**`previousEmphasis`** optional) (delegates wire semantics to **`buildLiveVisualSpec`**); **`compileReplayToGlassSceneV0`** (**`ReplaySceneCompileOptions.previousEmphasis`**) (index-ordered prefix vs pack — not live tail); **`liveVisualSpecFromScene`** = text/legend boundary (**incl.** **`boundedCompositionCaption`**, **`boundedEmphasisSummaryLine`**, **`boundedFocusCaptionLine`**, **`boundedStripReflowLine`**, **`boundedCompare*`** compare fields, strip metrics); **Vertical Slice v5** **`boundedSceneSelection.ts`** (hit map + inspector lines — **not** topology); **Vertical Slice v6** **`boundedSceneFocus.ts`** (selection → dim/emphasis — **not** graph); **Vertical Slice v7** **`boundedSceneFocusReflow.ts`** (selection → **layout** reflow — **not** topology); **Vertical Slice v8** **`boundedSceneCompare.ts`** (prev vs current bounded frame — **not** history); **Vertical Slice v9** **`boundedEvidenceDrilldown.ts`** / **`boundedEvidencePanel.ts`** (bounded tail/prefix evidence — **not** full trace); **Vertical Slice v10** **`boundedSceneCrosslink.ts`** (evidence ↔ selection/compare — **not** graph); **Vertical Slice v11** **`boundedTemporalLens.ts`** / **`boundedTemporalLensPanel.ts`** (recent bounded paints + baseline — **not** server log); **Vertical Slice v12** **`boundedEpisodes.ts`** / **`boundedEpisodesPanel.ts`** (rule-based episode cards — **not** causal AI); **Vertical Slice v13** **`boundedClaims.ts`** / **`boundedClaimsPanel.ts`** (bounded claim chips + receipt — **not** analysis theater); **Vertical Slice v14** — **`glass.receipt.v0`** bounded receipt record + claim↔evidence coupling — **not** export theater; **Drawable Primitives v0** (`DRAWABLE_PRIMITIVES_V0`, **`buildBoundedVisualGeometryPrimitives`**, **`applyBoundedSceneComposition`**, **`applyBoundedEmphasisOverlays`**, **`sceneToDrawablePrimitives`**) — deterministic **`fill_rect` / `stroke_rect`** lists in CSS px (no DOM), each with a **`semanticTag`** (`band_background`, `density_band`, `tick_slot_*`, `http_chip_*`, `band_frame`, **`composition_*`**, **`emphasis_*`**, **`compare_overlay_*`**, `state_rail_*`, `actor_cluster_*`, plus edge tags on WebGPU stroke expansion — **inspectable only**; does not assert extra visual truth); **semantic tag summary** (`listSemanticTagsForScene`, **`listSemanticTagsForPrimitives`** / **`primitiveTagsSummary`**, **`listSemanticTagsForWebGpuPrimitiveExpansion`**) — pure ordered tag arrays for golden tests / tooling (**no** new geometry; **not** a third semantic layer); **`exportTagSummaryToJsonLines`** — stable JSONL (`{"i", "tag"}` per line) for fixtures/diffs; **`__fixtures__/semantic_tag_summary_live_idle_v0.jsonl`** — one golden path for live idle primitive-level tags (**not** new semantic authority); **live** Canvas + WebGPU geometry both consume this list (renderers ignore tags for drawing; WebGPU expands **`stroke_rect`** to thin **`fill_rect`** quads; **text** stays Canvas 2D / overlay — not GPU text); **Tier B** `replayOnlyShell` renders **bounded scene Canvas** via **`renderLiveVisualOnCanvas(scene, …)`** (static replay: **Canvas 2D only** — WebGPU remains **live** path); **`?live=1`** visual stack tagged `data-scene="glass.scene.v0"` — **not** a full topology graph / **not** the final Phase-6 runtime scene |
| Phase 7 | `viewer/`: Tier B **static replay** — read-only `.glass_pack` (`loadGlassPack`) for **`glass.pack.v0.scaffold`** (`events.jsonl`) and **`glass.pack.v0.scaffold_seg`** (`events.seg` v1: magic `GLSSG001` + length-prefixed UTF-8 JSON per record, same envelope as JSONL); **replay session state** (`replayModel`: play/pause, seek/scrub, step, timeline + inspector), drag/drop + file open, sanitized/redaction summary surface; **index-ordered** playback (optional `ts_ns` display only; not wall-clock sync). **`?live=1` live-session shell** (`src/live/`): **Preflight** `GET /capabilities` (collector F-IPC configured, `live_session_delta_skeleton`, `session_delta_wire_v0`, `live_session_ingest`); **sessionStorage** for bridge URL / session id / delta-wire checkbox (**not** bearer token); **Connect disabled** when preflight succeeds with `collector_fipc.configured: false` (inline reason); **Disconnect** closes the active WebSocket (operator-initiated vs reconnect-superseded distinguished); **WS status** panel: phase (idle / connecting / open), **last close** `code` / `reason` / `wasClean` as received (no invented semantics), **Copy JSON** for WS block; **stale close** cleared on new **Connect**; **bounded live session log strip** (default **80** lines, oldest eviction; timestamped lines with **operator / preflight / ws / http**; concise wire summaries — no event payloads or tokens; **not** a durable audit trail); **Copy JSON** `glass_live_session_log_strip_v0`; **last HTTP reconcile** panel (operator **Refresh** vs automatic after `session_resync_required`, ISO time, ok/error); **live state presentation**: JSON summary of session id, bounded event count, last wire message + **append vs replace vs no tail change**, last resync / warning, reconcile trigger; **bounded WS event list** (oldest→newest) + honesty copy (sample / not full history; HTTP snapshot not merged into WS tail); **bounded live visual surface** — geometry from **`sceneToDrawablePrimitives(compileLiveToGlassSceneV0(…))`** (same **`buildLiveVisualMarkersLayout`** / slot semantics as before: three **slot** ticks for replace / append / resync **wire** — at most one **active**; optional **HTTP** chip + band frame strokes as primitives); **plain legend** under the pane (`formatLiveVisualLegendBlock` — R/A/Rz/HTTP labels + slots-not-timeline honesty); **WebGPU + hybrid text overlay** (`liveWebGpuProbe`, `tryInitWebGpuCanvas`, `renderLiveVisualWebGpuFrame`, `renderLiveVisualTextOverlayOnCanvas`): honest **`navigator.gpu`** probe + status line; **WebGPU** = **solid quads from drawable primitives** (strokes expanded — **no** GPU text); **transparent Canvas 2D** layer stacked above = **mode / tail / wire / reconcile / honesty** + HTTP chip label; **`paintLiveVisualSurface`** takes **`GlassSceneV0`**: hybrid when GPU frame **and** overlay succeed; GPU missing or frame fails → full **`renderLiveVisualOnCanvas`**; GPU OK but overlay **2d** context fails → full Canvas (labels never dropped silently); **`liveVisualProvenance`** — compact **provenance strip** (`buildLiveVisualProvenanceStrip` / `formatLiveVisualProvenanceStripText`) + **`serializeLiveVisualProvenanceStrip`** / **`toLiveVisualProvenanceExportV0`** (`glass_live_visual_provenance_v0` JSON, bounded fields, no auth secrets): **renderer** mode (hybrid / canvas_only / webgpu_unavailable / webgpu_failed_with_fallback), **wire** update mode, **`bounded_snapshot.snapshot_origin`** (or `none_yet`), **delta_wire** checkbox vs `session_delta_wire_v0`, last **HTTP reconcile** — viewer-known facts only (**not** topology / **not** timeline); **Copy JSON** / **Copy text** → clipboard + **session log** line (success / failure) — **not** a durable export; **not** Phase-6 scene / **not** topology; **fallback** if contexts missing; F-IPC **provisional**; **Copy JSON** for capabilities, reconcile, live-state summary, model, event tail; WebSocket `live_session_subscribe` → reducers record **`lastAppliedWire`** per message; **pure** `liveSessionLog` + `liveWsSessionStatus` + `liveVisualModel` / `liveVisualMarkers` / `liveVisualCanvas` / `liveWebGpuProbe` / `liveVisualWebGpu` / `liveVisualRenderer` + presentation helpers + tests |
| Collector | **`glass_collector` library** (retained loops use **`SnapshotStore::apply_retained_poll_continuity`** for prefix-extend vs replace; see `docs/contracts/live_session_ws_session_delta_v0.md`): raw `RawObservation`, adapters (`procfs_process` on Linux; **`fs_file_lane`** = bounded **directory poll** under optional `watch_root`, inactive when root unset), `procfs_session` + `ingest_procfs_raw_to_session_log`, **`file_session`** + `ingest_file_lane_raw_to_session_log` / `load_file_lane_observations_for_cli`, `procfs_ipc_feed` + **`file_lane_ipc_feed`** (per-RPC procfs / **file-lane** fixture or live tree → normalized JSON for F-IPC), **`procfs_retained_loop`** + **`file_lane_retained_loop`** (optional background polls → **bounded tail** in `SnapshotStore`; `retained_*_poll_tick` for tests), `RetainedPollMeta` (procfs + file-lane retained each have their own) + optional F-IPC `retained_snapshot_unix_ms`, `FidelityReport`, `pipeline`, IPC **types** + **provisional dev TCP** F-IPC (`ipc_dev_tcp`, `SnapshotStore`, `IpcDevTcpRuntime`, NDJSON `FipcBridgeToCollector` / `FipcCollectorToBridge`), privilege context. **`glass-collector` binary**: `capabilities`; **`sample-procfs`**; **`sample-file-lane`**; **`normalize-procfs`** / **`normalize-file-lane`**; **`export-procfs-pack`**; **`export-file-lane-pack`** (provisional share-safe file-lane export; F-05 path policy **not** final); **`ipc-serve`** → loopback TCP F-IPC (bounded snapshot RPC; seed JSON; **`--procfs-session`** / **`--file-lane-session`** per-RPC; **`--procfs-retained-session`** / **`--file-lane-retained-session`** + interval + max-events → retained store — **pairwise distinct** session ids across all four modes; **`live_session_ingest` stays false**) |
| Session / normalization / export | **`procfs_normalize`** → polling-honest process kinds. **`file_lane_normalize`** → directory-poll file kinds (`file_poll_snapshot`, `file_changed_between_polls`, `file_absent_in_poll_gap`, `file_seen_in_poll_gap`) — **not** `file_read` / `file_write` syscall truth. **`materialize_share_safe_procfs_pack_bytes`** / **`materialize_share_safe_file_lane_pack_bytes`**: same `sanitize_events_for_share` + manifest (`procfs_poll_dev_scaffold` vs **`file_lane_poll_dev_scaffold`**) + `write_glass_pack_to_vec`. Export lane (`sanitize_default.1.provisional`): **`attrs.exe`** → **`[REDACTED_ABS_PATH]`**; **file-lane** (provisional, F-05 open): **`attrs.relative_path`** → **`[REDACTED_REL_PATH]`**, **`attrs.watch_root`** → **`[REDACTED_ABS_PATH]`**, **`actor.entity_id`** `fs_poll_rel:` suffix → **`fs_poll_rel:[REDACTED]`** — not a final policy. **`normalize-file-lane`** unsanitized packs use **`file_lane_poll_dev_scaffold`**. Ingest / normalize CLI output stays **unsanitized** |
| Bridge | **Bounded-era F-04 frozen** (`docs/PHASE0_FREEZE_TRACKER.md`, `docs/contracts/bridge_session_snapshot_bounded_v0.schema.json`): opaque **`snapshot_cursor`**; **`bounded_snapshot.snapshot_origin`** disambiguates `v0:empty` (unknown vs per-RPC empty poll); **`resync_hint.reason`** tokens = `glass_bridge::resync::RESYNC_HINT_REASON_*`; optional non-normative **`detail`**; missing **`resync_hint`** = no extra bounded warning (not live-safe). **`live_session_ws`** (`docs/contracts/live_session_ws_skeleton_v1.md`, **`docs/contracts/live_session_ws_session_delta_v0.md`**): **`live_session_subscribe`** + **`F03OutboundQueue`**; **`session_delta`** v0 when bridge enables delta wire **and** client **`session_delta_wire: true`** **and** same bounded fingerprint **and** non-empty **`live_delta_events`** from **`collector_store`** F-IPC append-only tails (`live_delta_tail_v0` + **`store_revision`** watermark); otherwise **`session_snapshot_replaced`** / **`session_resync_required`** (incl. revision mismatch). F-03 coalescing applies to mixed snapshot+delta lines. **`GET /capabilities`**: `live_session_ingest` **true** only when F-IPC configured **and** delta wire enabled (honest). HTTP snapshot response **`live_session_ingest`** still comes from **collector** F-IPC payload (typically false). **F-IPC client** (`glass_bridge::ipc_client`): one **provisional** per-RPC deadline (`--collector-ipc-timeout-secs`); connect attempt capped by **`PROVISIONAL_FIPC_CONNECT_ATTEMPT_MAX`**; structured **`FipcClientError::bridge_error_code()`** maps to HTTP 503 JSON `error` + optional `fipc_phase` / `handshake_code` (not F-04). Collector **`ipc_dev_tcp`** rejects garbled post-handshake lines with **`invalid_request_line`** (fail-fast vs hanging). **`glass_bridge` binary** — loopback HTTP + WS; optional **`--collector-ipc-endpoint` / `--collector-ipc-secret`** |
| Graph | `graph_engine` minimal counting helper over events |
| HVT | `collector/config/hvt_rules.toml` (5 patterns) + CI count test |
| `tools/glass-pack` | **`glass-pack validate`** / **`info`** on `.glass_pack`; **`--strict-kinds`** (alias `--strict`); **`--expect-share-safe`** / **`--expect-raw-dev`** for manifest lane checks; **`--json`**. Rules implemented in `session_engine::pack` (`validate_share_safe_export_manifest`, `validate_raw_dev_pack_manifest`, `pack_artifact_lane_hint`) |
| Tests | See `docs/TEST_STRATEGY.md` |
| Retained snapshot demo | `docs/DEMO_RETAINED_SNAPSHOT.md`; `scripts/retained_snapshot_demo/` (fixture JSON + `demo.ps1` / `demo.sh`); subprocess smoke `integration_tests/tests/retained_snapshot_demo_smoke.rs`; **CI:** dedicated GitHub Actions job `Retained snapshot demo smoke (collector ↔ bridge F-IPC)` in `.github/workflows/ci.yml` |
| CI | `.github/workflows/ci.yml` |

## Intentionally scaffolded / deferred

- **Viewer `events.seg` does not imply:** compression, mmap/streaming session store, larger-than-F-07 browser caps beyond current decode, or any live ingest path.
- Segment compression, mmap session store, replay indexes (beyond v1 record format).
- Real eBPF attach, **fanotify/inotify-class live streams**, netlink (**`network_lane` / `linux_ebpf` remain capability-only or unsupported polls**)
- **Not in procfs lane:** kernel-exact spawn/exit, guaranteed parent/child temporal truth, namespace/container boundaries (deferred to eBPF / future work)
- Non-procfs raw → normalized mapping (**eBPF, network** lanes deferred). **Fs file lane** raw → normalized is **landed** for directory-poll semantics only (see `file_lane_normalize`).
- Bridge **final live ingest** (push-based collector → bridge, durable delta log, live viewer client): **not** in v0; F-IPC remains **provisional TCP**. **`live_session_ws`** implements **F-03 v0** per-connection queue + backpressure; it **polls** F-IPC and can surface **bounded append-only tails** for **`collector_store`** sessions when the bounded prefix is stable — **not** a global history
- **Full** Glass topology / process-tree **runtime** scene (Phase-6+) — distinct from **Scene System v0** (bounded compilers + same `LiveVisualSpec` raster path); live **WebGPU hybrid** remains geometry + Canvas text
- Stricter browser-side limits (size caps, streaming JSONL) beyond current ZIP + line-length bound (F-07)

## Blocked on human decisions

See `docs/PHASE0_FREEZE_TRACKER.md` — Open freeze decisions.

**Bounded-era F-04** (snapshot cursor + `resync_hint` on `GET /sessions/:id/snapshot`) is **closed**. **F-03 v0** queue + **`session_delta` v0** with real **`collector_store`** tails — **`live_session_ws`** (see `docs/F03_LIVE_BACKLOG_FREEZE_PROPOSAL.md`, `docs/contracts/live_session_ws_session_delta_v0.md`). **Still provisional / next:** durable push ingest, **F-04 live-era** HTTP extensions, F-IPC transport freeze, F-05 sanitization, etc.

## Next engineering steps

1. Close remaining Phase 0 items (golden method F-01, sanitization F-05). **Viewer:** Vertical Slice v0 surfaces replay/live parity in UI; continue **Scene System v0** + **Drawable Primitives v0** (**replay** + **live** → **`sceneToDrawablePrimitives`** → Canvas / WebGPU geometry; **`listSemanticTagsForScene`** / **`exportTagSummaryToJsonLines`** + small **`__fixtures__`** JSONL goldens — **not** a second semantic authority); text/legend still via **`liveVisualSpecFromScene`**; **`?live=1`** still exposes append/replace/resync/reconcile, **Disconnect**, **WS close** reporting, **bounded session log strip**, **bounded visual** + **provenance strip** + **Copy JSON/text**, **Canvas 2D** + **WebGPU hybrid** + **slot ticks** + **HTTP** chip + **plain legend** + **GPU status** (honest — **not** topology / **not** full history). **Next live-session engineering:** durable push ingest beyond **`SnapshotStore`** append tails; **additive** live-era HTTP / WS fields only — **do not** break frozen bounded HTTP tokens or opaque cursor contract without a version bump. **Visual next:** optional additional JSONL fixtures (e.g. WebGPU expansion stream) when needed; map additional **Scene v0** nodes/zones into tagged drawables when honestly available (still bounded); Phase-6 **topology-aware** scene only when data exists — **F-IPC transport remains provisional** (not frozen here).
2. **Optional:** tighten browser-side pack/ZIP size caps and streaming decode if large packs become a product requirement (F-07 line/record bound already enforced per event).
3. **Optional next file-lane step:** `notify`/`fanotify` substrate (still honest capability gating), F-IPC bounded snapshots for file-lane sessions, and/or **share-safe export** rules for file paths in `attrs` (F-05). Extend normalization + share-safe export for **eBPF / network** when those adapters land; IPC socket when F-IPC closes; tighten F-05 / path heuristics with human review; optional **`glass-pack`** checks for additional pack lanes when they land.
4. Replace **provisional TCP** F-IPC with human-frozen transport (Unix socket / peer creds); honest WS deltas + resync harness (drops/dupes/reorder); live viewer client (Phase 5–6); WebGPU scene replay (Phase 6) — separate track.
