# `glass_bridge`

Unprivileged local API **contracts** (resync types, provisional thresholds). HTTP/WebSocket server is **not** implemented in this bootstrap.

See `docs/PRIVILEGE_SEPARATION.md` and `docs/REPO_BOUNDARIES.md`.
