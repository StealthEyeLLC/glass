# Pack fixtures

Packs can be generated with `glass-pack` / `session_engine::write_glass_pack` for tests.

Binary-checked-in `.glass_pack` files are optional; round-trip tests generate ZIPs in temp dirs to avoid drift.
