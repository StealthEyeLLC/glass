# Schema migrations

Version bumps for `glass.event.v*` and pack formats are recorded here (migration notes + tooling).

`glass.pack.v0.scaffold` uses ZIP + `manifest.json` + `events.jsonl`. Binary `events.seg` replaces `events.jsonl` only after Phase 0 freeze approval (`docs/PHASE0_FREEZE_TRACKER.md`).
