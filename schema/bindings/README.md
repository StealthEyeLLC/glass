# Generated bindings (placeholder)

Rust types live in `session_engine::event` and must stay aligned with `../glass_event_schema.json`.

Planned: `cargo xtask schema-codegen` or similar to emit TypeScript types for `viewer/` (Phase 1).

Do not hand-edit generated output here without updating the schema and Rust types in the same change.
