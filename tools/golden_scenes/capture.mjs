#!/usr/bin/env node
/**
 * Placeholder — does not render. Exits 0 so CI proves the harness path exists.
 * Replace with real capture when Phase 6 golden pipeline is implemented.
 */
console.log("golden_scenes: scaffold (no GPU capture)");
process.exit(0);
