# Collector

`glass-collector` exits with code **2** and prints a message — **no synthetic telemetry**. Phase 2 implements Linux sensors per the locked spec.

HVT path rules: `config/hvt_rules.toml` (count enforced by CI).
